@Library('lexicon-lib') _

lexiconReleasePipeline(
    artifactGlob: '*.pk3',          // optional if *.pk3 is the default
    githubTokenId: 'github-app-mythotic'   // optional if that's the ID you used
)
